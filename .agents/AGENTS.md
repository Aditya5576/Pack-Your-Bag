# Custom Rules & Preferences

- **Testing Preference**: When the user requests to run tests with agents, always delegate test execution to subagents/agent tools. Do not run them directly in the parent context.
